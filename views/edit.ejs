<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Book notes</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/styles/main.css" />
  </head>

  <body>
    <header class="container-sm py-3 head">
      <div class="text-start"><h1>Book notes</h1></div>
      <div class="d-flex justify-content-center align-items-center">
        <form
          class="col-auto searchBar"
          role="search"
          method="post"
          action="/search/"
        >
          <input
            type="search"
            name="bookSearch"
            placeholder="Enter book name, author or isbn"
            class="searchBar"
          />
        </form>
        <button
          type="button"
          class="btn ml-4"
          onclick="window.location.href=`/`"
        >
          Home
        </button>
      </div>
    </header>

    <!-- End of header -->

    <div class="container-sm text-start content">
      <!-- start of DB entries -->
      <div class="d-flex">
        <img src="<%= imageUrl %>" class="book-image" />
        <div class="bookInfo">
          <h3 class="my-3"><%= book.title %> by <%= book.author %></h3>
          <div class="buttons d-flex">
            <h4 class="rating">
              Date read: <%= book.date_read %> How strongly I recommend it: <%=
              book.rating %>/10
            </h4>
          </div>
          <form action="/save/<%= book.id %>" method="post">
            <textarea name="editField" class="feedback">
<%= book.comments %></textarea
            >
            <input type="submit" value="Save changes" />
            <input type="hidden" name="book_id" value="<%= book.id %>" />
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
