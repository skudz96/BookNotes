<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Book notes</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/styles/main.css" />
  </head>

  <body>
    <header class="container-sm py-3 head">
      <div class="text-start"><h1>Book notes</h1></div>
      <div class="d-flex justify-content-center align-items-center">
        <form
          class="col-auto searchBar"
          role="search"
          method="post"
          action="/search"
        >
          <input
            type="search"
            name="bookSearch"
            placeholder="Enter book name, author or isbn"
            class="searchBar"
          />
        </form>
        <button type="button" class="btn" onclick="window.location.href=`/`">
          Home
        </button>
      </div>
    </header>

    <!-- End of header -->

    <div class="container-sm text-start content">
      <!-- start of DB entries -->
      <div class="row py-1">
        <div class="col-md-12">
          <h3 class="my-3"><%= title %> by <%= author %></h3>
          <img src="<%= imageUrl %>" class="book-image" />
          <form action="/submit/<%= isbn %>" method="post">
            <textarea
              name="review"
              placeholder="Review:"
              class="feedback"
            ></textarea>
            <p>test</p>
            <input type="submit" value="Submit and Add to collection" />
            <input value="<%= title %>" class="hidden" name="title" />
            <input value="<%= author %>" class="hidden" name="author" />
            <input value="<%= imageUrl %>" class="hidden" name="link" />
            <input value="<%= isbn %>" class="hidden" name="isbn" />
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
